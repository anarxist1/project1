// ConsoleApplication9.cpp: определяет точку входа для консольного приложения.
//

// ConsoleApplication7.cpp: определяет точку входа для консольного приложения.
//

#include "stdafx.h"
#include <math.h>
#include <iostream>
#include <cstdlib>
#include <locale>
using namespace std;
long Perevert(long);

int fun1(int a)
{
	int sum = 0;
	a = Perevert(a);

	while (a != 0)
	{
		a /= 10;
		
		sum = sum + a % 10; // запись сокращена и полная запись будет sum = sum + a % 10;
							 //с начало в этом примере производиться операция деление по модулю-> a % 10, потом результат прибавляется в sum
		 // здесь просто число делиться на десить и таким образом уходит последнее число
		a /= 10;	  // вообще чтобы тебе было легче понять как работает программа когда ты ее написал в Visual Studio то жми с начало F10 потом F11 чтобы она 
				  // шла по строкам кода тебе так будет понятно как работает строки кода
	}

	return sum;
}


int main()
{
	setlocale(LC_ALL, "Russian"); // русский в консоле 
	int  num1 = 0; // переменные 
	cout << "Введите число:" << endl; // вводим первое значение
	cin >> num1;
	cout << "Результат:" << fun1(num1) << endl; // вызов функции и передачи чисел в эту функцию, а она нам возращает число
	system("pause");
	return 0;
}

long Perevert(long x)
{
	long y = 0;
	while (x)
	{
		y = y * 10 + x % 10;
		x /= 10;
	}
	return y;
}
